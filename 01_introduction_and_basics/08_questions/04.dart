// Write a program in Dart that finds simple interest. Formula= (p * t * r) / 100

void main() {
  double p = 100;
  double t = 5;
  double r = 8.5;

  var Formula = (p * t * r) / 100;
  print(Formula);
}